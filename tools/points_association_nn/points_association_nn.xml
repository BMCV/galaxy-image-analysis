<tool id="ip_points_association_nn" name="Perform linking in time series (nearest neighbors)" version="0.0.5" profile="20.05"> 
    <description></description>
    <macros>
        <import>creators.xml</import>
    </macros>
    <creator>
        <expand macro="creators/bmcv"/>
    </creator>
    <edam_operations>
        <edam_operation>operation_3443</edam_operation>
    </edam_operations>
    <xrefs>
        <xref type="bio.tools">galaxy_image_analysis</xref>
    </xrefs>
    <requirements>
        <requirement type="package" version="2.3.5">numpy</requirement>
        <requirement type="package" version="2.2.2">pandas</requirement>
        <requirement type="package" version="0.25.2">scikit-image</requirement>
    </requirements>
    <required_files>
        <include type="glob" path="*.py"/>
    </required_files>
    <command>
    <![CDATA[

        mkdir ./output &&
        python '$__tool_directory__/points_association_nn.py'
             '$fn_in'
             ./output
             '$nbpx'
             '$thres'
             '$minlen'

    ]]>
    </command>
    <inputs>
        <param name="fn_in" type="data" format="tsv,tabular" label="Coordinates (and intensities) of input points (tsv tabular)"/>
        <param name="nbpx" type="integer" value="6" optional="true" min="1" max="30" label="Neighborhood size (in pixel) for nearest neighbor search"/>
        <param name="thres" type="float" value="25" optional="true" min="0.0" max="99.9" label="Minimum relative track intensity" help="Tracks with all intensities lower than certain percentage (%) of the global maximal intensity will be discarded" />
        <param name="minlen" type="float" value="50" optional="true" min="1.0" max="100.0" label="Minimum length of tracks (in % of the full sequence length)"/>
    </inputs>
    <outputs>
        <collection type="list" name="output" label="Tracks in ${on_string}">
            <discover_datasets directory="output" pattern="__name__" format="tsv"/>
        </collection>
    </outputs>
    <tests>
        <test>
            <param name="fn_in" value="input1.tsv"/>
            <param name="nbpx" value="6"/>
            <param name="thres" value="25"/>
            <param name="minlen" value="50"/>
            <output_collection name="output" type="list" count="70">
                <element name="track1.tsv" value="output1/track1.tsv" ftype="tsv" compare="diff"/>
                <element name="track2.tsv" value="output1/track2.tsv" ftype="tsv" compare="diff"/>
                <element name="track3.tsv" value="output1/track3.tsv" ftype="tsv" compare="diff"/>
            </output_collection>
        </test>
    </tests>
    <help>

        **Performs linking in tabular lists of points from multi-frame time series.**

        This tool associates points in consecutive frames using the nearest neighbor algorithm.

    </help>
    <citations>
        <citation type="doi">10.1097/j.pain.0000000000002642</citation>
    </citations>
</tool>
